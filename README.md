# This repo will consist of a few neural network projects that I made to learn how does a Neural Network actually works.
## There will be a few number of logic gates at start but later on there will be intresting models as well~!

## 1.  AND GATE:-
The file of AND_Gate.py can simulate how an AND gate is simulated by using Neural Networks.

## 2. OR GATE:-
Just like how the AND_Gate file simulates an AND gate, the OR_gate file will simulate how an OR gate is simulated using Neural Networks.

## 3. NAND Gate:-
Similarly the NAND gate is implemented

### ***NOTE: Though I am using the word simulated but here the Neural Network is solving it and not simulating it, trying to capture the underlying patterns, and minimizing the errors***
